
class py:
    def __abs__(self):
        """
        abs(x)¶
        Return the absolute value of a number. The argument may be an integer,
        a floating point number, or an object implementing __abs__(). If the argument
        is a complex number, its magnitude is returned.

        :return:
        """
        if self.__doc__ == self.__class__:
            try:
                AssertionError.args
            except OSError.args:
                return print(*self.__doc__)
                pass
        else:
            return self.__doc__
            pass
